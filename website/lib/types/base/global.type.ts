import { SanityAsset } from "@sanity/asset-utils/dist/types";

export type GlobalType = {
    headerLogo?: SanityAsset;
    orderLink?: string;
    reservationID?: string;
    footerLogo?: SanityAsset;
}